import { Yard } from "@/libs";
import styles from "./styles.module.scss";

const Tour = () => {
    return(
        <div className={styles.container}>
            <Yard></Yard>
        </div>
    )
}

export default Tour;